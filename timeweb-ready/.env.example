# ─── PostgreSQL (обязательно для production) ───────────────────
# Вариант 1: полная строка подключения (приоритет)
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Вариант 2: отдельные параметры (используются если DATABASE_URL не задан)
# PG_HOST=your-pg-host.timeweb.cloud
# PG_PORT=5432
# PG_DATABASE=your_db
# PG_USER=your_user
# PG_PASSWORD=your_password
# PG_SSL=true

# ─── Общие ─────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
