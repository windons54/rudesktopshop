# Инструкция по тестированию функционала банка

## Проверка сохранения данных в БД

### 1. Создание вклада (администратор)

1. Войдите как администратор
2. Перейдите в Настройки → Банк
3. Создайте вклад:
   - Название: "Выгодный"
   - Срок: 7 дней
   - Ставка: 10%
4. **Обновите страницу (F5)**
5. ✅ Вклад должен остаться в списке

### 2. Открытие вклада (пользователь)

1. Перейдите на страницу "Банк" в главном меню
2. Выберите созданный вклад
3. Введите сумму (например, 100)
4. Посмотрите прогнозируемый доход (+10 монет)
5. Нажмите "Открыть вклад"
6. ✅ Проверьте списание монет с баланса
7. **Обновите страницу (F5)**
8. ✅ Активный вклад должен отображаться

### 3. Калькулятор дохода

1. Нажмите "🧮 Калькулятор дохода"
2. Выберите вклад
3. Введите сумму
4. ✅ Проверьте правильность расчета:
   - Доход = Сумма × Ставка ÷ 100
   - Пример: 100 × 10% = 10 монет

### 4. Автоматическое завершение вклада

**Вариант А: Тестирование с коротким сроком**
1. Создайте вклад со сроком 0.001 дня (≈1.5 минуты)
2. Откройте вклад
3. Подождите ~2 минуты
4. ✅ Должно появиться уведомление о завершении
5. ✅ Монеты + проценты должны вернуться на баланс
6. ✅ Вклад должен переместиться в архив

**Вариант Б: Изменение даты в данных (для быстрой проверки)**
1. Откройте DevTools (F12)
2. В Console выполните:
```javascript
// Получить текущие депозиты
let deps = JSON.parse(localStorage.getItem('_store_cm_user_deposits') || '[]');
// Установить дату завершения на текущий момент
deps = deps.map(d => d.status === 'active' ? {...d, endsAt: Date.now() - 1000} : d);
// Сохранить
localStorage.setItem('_store_cm_user_deposits', JSON.stringify(deps));
// Обновить страницу
location.reload();
```
3. Через 5 секунд вклад автоматически завершится

### 5. Проверка архива

1. После завершения вклада перейдите в раздел "Архив"
2. ✅ Завершенный вклад должен отображаться
3. ✅ Отображается правильный доход
4. ✅ Указана дата завершения
5. **Обновите страницу (F5)**
6. ✅ Архив сохраняется

### 6. Проверка синхронизации (множественные вкладки)

1. Откройте приложение в двух вкладках браузера
2. В первой вкладке откройте вклад
3. ✅ Во второй вкладке через ~3 секунды должны обновиться:
   - Баланс пользователя
   - Список активных вкладов

### 7. Проверка PostgreSQL (если используется)

Если у вас настроен PostgreSQL:

```sql
-- Проверить наличие записи с вкладами
SELECT key, value FROM kv WHERE key = 'cm_deposits';

-- Проверить наличие записи с депозитами пользователей
SELECT key, value FROM kv WHERE key = 'cm_user_deposits';
```

### 8. Проверка редактирования вкладов (администратор)

1. В админ-панели нажмите ✏️ на созданном вкладе
2. Измените название/срок/ставку
3. Сохраните
4. **Обновите страницу (F5)**
5. ✅ Изменения должны сохраниться

### 9. Проверка удаления вкладов (администратор)

1. В админ-панели нажмите 🗑️ на вкладе
2. Подтвердите удаление
3. **Обновите страницу (F5)**
4. ✅ Вклад не должен отображаться

## Ожидаемое поведение

### ✅ Корректная работа:
- Данные сохраняются при обновлении страницы
- Вклады автоматически завершаются по истечении срока
- Проценты начисляются правильно
- Баланс обновляется корректно
- Архив сохраняется
- Данные синхронизируются между вкладками

### ❌ Возможные проблемы:

**Проблема:** Вклады исчезают после обновления
**Решение:** Проверьте настройки базы данных в разделе "База данных"

**Проблема:** Вклад не завершается автоматически
**Решение:** Убедитесь, что вкладка с приложением открыта (проверка происходит каждые 5 секунд)

**Проблема:** Данные не синхронизируются между вкладками
**Решение:** Проверьте, что polling включен и работает (обновление каждые 3 секунды)

## Технические детали

### Где хранятся данные:

1. **PostgreSQL (при наличии):**
   - Таблица: `kv`
   - Ключ для вкладов: `cm_deposits`
   - Ключ для депозитов: `cm_user_deposits`

2. **localStorage (fallback):**
   - `_store_cm_deposits`
   - `_store_cm_user_deposits`

### Формат данных:

**cm_deposits:**
```json
[
  {
    "id": 1709012345678,
    "name": "Выгодный",
    "duration": 7,
    "rate": 10,
    "createdAt": 1709012345678
  }
]
```

**cm_user_deposits:**
```json
[
  {
    "id": 1709012345678,
    "user": "username",
    "depositId": 1709012345678,
    "depositName": "Выгодный",
    "amount": 100,
    "rate": 10,
    "duration": 7,
    "createdAt": 1709012345678,
    "endsAt": 1709617145678,
    "status": "active"
  }
]
```
