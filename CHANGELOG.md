# Внесенные изменения

## Обновление от 27.02.2026 (v2) - Улучшения UX и логики

### 1. Калькулятор дохода теперь на главной странице Банка

**Было:** Калькулятор открывался в модальном окне по кнопке  
**Стало:** Калькулятор отображается сразу на странице банка первым блоком

#### Изменения:
- Удалена кнопка "🧮 Калькулятор дохода" из header
- Калькулятор встроен как первый раздел на странице
- Автоматическая инициализация первым доступным вкладом
- Улучшена UX - пользователь сразу видит все инструменты

### 2. Подтверждение автоматического возврата средств

**Уже работает правильно:**
- ✅ При завершении вклада автоматически начисляется сумма вклада + проценты
- ✅ Средства возвращаются на баланс пользователя
- ✅ Вклад перемещается в архив со статусом "completed"
- ✅ Пользователь получает уведомление о начислении

Реализация в строках 7444-7461 файла `App.jsx`:
```javascript
const profit = Math.round(ud.amount * ud.rate / 100);
const total = ud.amount + profit;
newUsers[ud.user].balance = (newUsers[ud.user].balance || 0) + total;
```

### 3. Логика аукциона: проверка средств победителя

**Новая логика завершения аукциона:**

При завершении аукциона система:
1. Проверяет баланс последнего участника (с максимальной ставкой)
2. Если средств недостаточно → переходит к предыдущей ставке
3. Проверяет всех участников в обратном порядке
4. Находит первого участника с достаточным балансом
5. Объявляет его победителем и списывает средства

**Если ни у кого нет средств:**
- Аукцион завершается без победителя
- Отображается сообщение: "⚠️ Аукцион завершён без победителя (недостаточно средств у участников)"

#### Технические детали:

**Алгоритм поиска победителя** (строки 1917-1987):
```javascript
// Проходим по ставкам в обратном порядке
for (let i = auction.bids.length - 1; i >= 0; i--) {
  const bid = auction.bids[i];
  const u = users[bid.user];
  
  // Проверяем баланс
  if (u && (u.balance || 0) >= bid.amount) {
    winnerBid = bid;
    winnerUser = bid.user;
    break;
  }
}
```

**Новые поля аукциона:**
- `finalWinner` - username реального победителя
- `finalAmount` - финальная сумма ставки победителя
- `noWinner` - true если ни у кого не хватило средств

**Отображение результатов:**
- 🏆 С победителем: "Победитель: Имя — 500 монет"
- ⚠️ Без победителя: красный баннер с предупреждением

---

## Обновление от 27.02.2026 (v1) - Исправление хранения данных

### Критическое исправление: Сохранение данных вкладов в БД

**Проблема:** Данные о вкладах пользователей хранились только в локальном состоянии компонента и терялись при обновлении страницы.

**Решение:** Интегрировано хранение данных через глобальную систему storage:

#### Изменения в `components/App.jsx`:

1. **BankAdminTab** - теперь получает данные через props:
   - `deposits` - массив типов вкладов
   - `saveDeposits` - функция сохранения в БД
   - Удалено локальное состояние `useState(() => storage.get(...))`

2. **BankPage** - теперь получает данные через props:
   - `deposits` - доступные типы вкладов
   - `userDeposits` - депозиты пользователей
   - `saveUserDeposits` - функция сохранения в БД
   - Удалено локальное состояние

3. **Глобальное хранение:**
   - `cm_deposits` - сохраняется в PostgreSQL/localStorage
   - `cm_user_deposits` - сохраняется в PostgreSQL/localStorage
   - Данные автоматически синхронизируются между пользователями
   - Данные сохраняются при обновлении страницы
   - Polling каждые 3 секунды обновляет данные во всех открытых вкладках

#### Что теперь работает:
- ✅ Вклады сохраняются в базе данных
- ✅ Данные не теряются при обновлении страницы
- ✅ Вклады видны после перезахода в систему
- ✅ Автоматическое завершение вкладов работает корректно
- ✅ Данные синхронизируются между вкладками браузера
- ✅ История вкладов сохраняется в архиве

---

## 1. Исправление кнопок "Купить" и "Подробнее"

### Файл: `styles/globals.css`
- Добавлено свойство `min-height: 36px` для класса `.pc-action-btn`
- Теперь кнопки "Купить" и "Подробнее" имеют одинаковую высоту на всех карточках товаров

## 2. Добавлена вкладка "Банк" в админке

### Файл: `components/App.jsx`

#### Новая вкладка в админ-панели:
- Добавлена вкладка "🏦 Банк" в список `SIDEBAR_TABS` (строка ~5255)
- Создан компонент `BankAdminTab` для управления вкладами

#### Функционал администратора:
- Создание вкладов с параметрами:
  - Название вклада
  - Срок (в днях)
  - Процентная ставка (%)
- Редактирование существующих вкладов
- Удаление вкладов
- Просмотр списка всех созданных вкладов

## 3. Добавлена вкладка "Банк" в главное меню

### Файл: `components/App.jsx`

#### Главное меню:
- Добавлена кнопка "Банк" в навигацию между "Голосования" и "Задания" (строка ~901)
- Создан компонент `BankPage` для пользовательской части

#### Функционал пользователя:
1. **Открытие вклада:**
   - Выбор типа вклада из списка созданных администратором
   - Указание суммы для вложения
   - Списание монет с баланса при открытии вклада
   - Отображение прогнозируемого дохода перед открытием

2. **Калькулятор дохода:**
   - Кнопка "🧮 Калькулятор дохода" в шапке страницы
   - Выбор вклада
   - Ввод суммы
   - Моментальный расчет:
     - Сумма дохода
     - Итоговая сумма к получению

3. **Отображение активных вкладов:**
   - Название вклада
   - Вложенная сумма
   - Процентная ставка
   - Оставшееся время до завершения
   - Прогнозируемый доход

4. **Автоматическое завершение вклада:**
   - Проверка каждые 5 секунд
   - При истечении срока:
     - Начисление процентов
     - Возврат суммы + доход на баланс пользователя
     - Перемещение вклада в архив
     - Уведомление пользователя

5. **Архив вкладов:**
   - История завершенных вкладов
   - Отображение полученного дохода
   - Дата завершения

## 4. Исправлено отображение названия монет в голосованиях

### Файл: `components/App.jsx`

Везде в разделе голосований заменено:
- "🪙 монет" → используется системное название валюты `getCurrName(currency)`
- Добавлен компонент `<CurrencyIcon currency={currency} />` для отображения иконки валюты

#### Места изменений:
- Список активных голосований (строка ~7195)
- Карточки завершенных голосований (строка ~7226)
- Модальное окно голосования - награда (строка ~7262)
- Модальное окно - выданная награда (строка ~7268)

## 5. Структура данных

### Новые поля в storage:
- `cm_deposits` - массив типов вкладов (создаются админом)
  ```javascript
  {
    id: timestamp,
    name: string,           // Название вклада
    duration: number,       // Срок в днях
    rate: number,          // Процентная ставка
    createdAt: timestamp
  }
  ```

- `cm_user_deposits` - массив депозитов пользователей
  ```javascript
  {
    id: timestamp,
    user: string,          // username пользователя
    depositId: number,     // ID типа вклада
    depositName: string,   // Название вклада
    amount: number,        // Вложенная сумма
    rate: number,          // Процентная ставка
    duration: number,      // Срок в днях
    createdAt: timestamp,
    endsAt: timestamp,     // Дата завершения
    status: 'active' | 'completed',
    completedAt: timestamp // Когда был завершен
  }
  ```

## Технические детали

### Добавленные функции:
- `saveDeposits(dp)` - сохранение типов вкладов
- `saveUserDeposits(ud)` - сохранение депозитов пользователей

### Компоненты:
- `BankAdminTab({ notify })` - административная панель управления вкладами
- `BankPage({ currentUser, users, saveUsers, notify, currency })` - пользовательская страница банка

### Особенности реализации:
- Автоматическая проверка истекших вкладов через `setInterval` каждые 5 секунд
- Расчет дохода: `Math.round(amount * rate / 100)`
- Использование системной валюты для отображения везде
- Реактивное обновление состояния при завершении вкладов

## Установка и запуск

Проект готов к использованию. Все изменения внесены в существующую кодовую базу без добавления новых зависимостей.

```bash
# Если нужно установить зависимости заново:
npm install

# Запуск проекта:
npm run dev
```

## Примечания

- Все данные хранятся в существующей системе storage (PostgreSQL или localStorage)
- Совместимость с существующими функциями не нарушена
- Код полностью интегрирован с текущей архитектурой приложения
